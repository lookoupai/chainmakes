# ========================================
# ChainMakes 配置文件
# ========================================
# 复制此文件为 .env: cp .env.example .env

# ========================================
# 安全密钥 (必须修改!)
# ========================================
# 生成方法: openssl rand -hex 32
SECRET_KEY=change-this-to-a-random-secret-key
ENCRYPTION_KEY=change-this-to-a-random-encryption-key

# ========================================
# OKX API 配置
# ========================================
OKX_IS_DEMO=true
OKX_API_KEY=your-okx-api-key
OKX_API_SECRET=your-okx-api-secret
OKX_PASSPHRASE=your-okx-passphrase

# 代理配置
# 国内需要设置: http://127.0.0.1:7890
# 海外留空即可
OKX_PROXY=

# ========================================
# 可选配置
# ========================================

# 数据库 (默认使用 SQLite)
# 如需 PostgreSQL,取消注释并启动: docker-compose --profile postgres up
# DATABASE_URL=postgresql://chainmakes:password@postgres:5432/chainmakes
# DATABASE_URL_ASYNC=postgresql+asyncpg://chainmakes:password@postgres:5432/chainmakes
# POSTGRES_PASSWORD=your-password

# 应用配置
ENVIRONMENT=production
LOG_LEVEL=INFO

# 前端配置
CORS_ORIGINS=http://localhost:3000
FRONTEND_PORT=80
